/* --- Global Dependencies --- */
import styled from 'styled-components'
import {
  color,
  fontSize,
  fontWeight,
  letterSpacing,
  textAlign,
  opacity,
  space,
} from 'styled-system'

/* --- Styled --- */
const Label = styled.label`
  ${color}
  ${fontSize}
  ${fontWeight}
  ${letterSpacing}
  ${textAlign}
  ${opacity}
  ${space}
`

/* --- Component --- */
export default Label